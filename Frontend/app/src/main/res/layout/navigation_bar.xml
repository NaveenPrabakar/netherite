<?xml version="1.0" encoding="utf-8"?>

<androidx.cardview.widget.CardView 
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:backgroundTint="@color/light_gray"
    app:cardElevation="0dp"
    app:cardCornerRadius="22dp"
    android:layout_marginHorizontal="10dp"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintBottom_toTopOf="@id/navbar"
    android:layout_height="wrap_content"
    android:layout_width="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/navbarMAINMAINMAIN"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="@color/light_gray"
        android:paddingVertical="8dp"
        android:elevation="4dp"
        android:gravity="center">

        <!-- Microphone Icon -->
        <ImageButton
            android:id="@+id/NAVocrButton"
            android:layout_width="0dp"
            android:layout_height="30dp"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_camera"
            android:contentDescription="@string/ocr" />

        <!-- Home Icon -->

        <!-- Edit Icon -->
        <ImageButton
            android:id="@+id/NAVgalleryNavButt"
            android:layout_width="0dp"
            android:layout_height="30dp"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/ocr"
            android:src="@drawable/ic_gallery" />

        <!-- Settings Icon -->

        <ImageButton
            android:id="@+id/NAVhomeButton"
            android:layout_width="0dp"
            android:layout_height="30dp"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/home"
            android:src="@drawable/home" />

        <ImageButton
            android:id="@+id/NAVeditButton"
            android:layout_width="0dp"
            android:layout_height="30dp"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/create_note"
            android:src="@drawable/navbar_create_note" />

        <ImageButton
            android:id="@+id/NAVsettingsButt"
            android:layout_width="0dp"
            android:layout_height="30dp"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/ocr"
            android:src="@drawable/ic_settings" />

    </LinearLayout>
</androidx.cardview.widget.CardView>

